postgres db and redis running in docker, connection strings already set up
ef core for data access
tickerq as job scheduler
for parsing rss feeds: just use System.Xml.Linq and XDocument.Parse
NEVER EVER RUN POWERSHELL COMMANDS TO READ THE CONTENTS OF INSTALLED NUGET PACKAGES