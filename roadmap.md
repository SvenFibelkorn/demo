I need a scheduled job that parses rss feeds and adds new articles to the postgres db
I need a scheduled job that looks for article entries in the db, that do not have embeddings yet
I need a way to trigger the jobs manually